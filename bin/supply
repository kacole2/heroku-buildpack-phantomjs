#!/bin/bash
set -euo pipefail

BUILD_DIR=$1
CACHE_DIR=$2

export BUILDPACK_DIR=`dirname $(readlink -f ${BASH_SOURCE%/*})`

echo "-----> Running BUILDPACK_DIR"

$BUILDPACK_DIR/bin/compile "$BUILD_DIR" "$CACHE_DIR" 0